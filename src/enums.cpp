#include "enums.h"

/*void writeString(ofstream& os, string text)
{
    os << text.size();
    os << text;
}

string loadString(ifstream& is)
{
    size_t textLength;
    is >> textLength;
    string text;
    for(size_t i = 0; i < textLength; i++)
    {
        char c;
        is >> c;
        text += c;
    }
    return text;
}*/